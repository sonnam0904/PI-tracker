# Database driver: sqlite | postgres | mysql
DB_DRIVER=sqlite

# sqlite
DB_SQLITE_PATH=taskmanager.db

# postgres / mysql
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=taskmanager
DB_SCHEMA=
